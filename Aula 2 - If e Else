#include <stdio.h>
#include <string.h>
int main (){


    //Atividade 1.
    /*int idade;
    printf ("Digite sua idade: ");
    scanf ("%d" , & idade);
    
    if (idade >= 18){
        printf ("Você pode entrar!");
    }
    else {
        printf ("Você não pode entrar!");
    }*/

    //Atividade 2.
    /*float nota1;
    float nota2;
    float nota3;
    float media;
    
    printf ("digite sua nota do primeiro trimestre: ");
    scanf ("%f", & nota1);
    printf ("digite sua nota do segundo trimestre: ");
    scanf ("%f", & nota2);  
    printf ("digite sua nota do terceiro trimestre: ");
    scanf ("%f", & nota3); 
    
    if (nota1 < 0 || nota1 > 10 || nota2 < 0 || nota2 > 10 || nota3 < 0 || nota3 > 10){
        printf ("Digite uma nota válida!");
        return 0;
    }
    
    media = (nota1 + nota2 + nota3) / 3;
    
    if (media >= 6){
        printf ("Com a sua média de %.2f, você está aprovado! ", media);
    }
    else {
        printf ("Com a sua média de %.2f, você está reprovado! ", media);
    }*/

    //Atividade 3.
    /*int idade;
    float renda;
    
    printf ("Qual a sua idade? ");
    scanf ("%d", & idade);
    printf ("Qual a sua renda? ");
    scanf ("%f", & renda);
    
    if (idade >= 25 && renda >= 2000){
        printf ("Empréstimo liberado!");
    }
    else {
        printf ("Empréstimo negado!");
    }*/

    //Atividade 4.
    /*int tipo;
    char ocupacao[20];
    printf ("Portal da Escola: \n");
    printf ("Digite 1 se você for aluno ou 2 se for professor: ");
    scanf ("%d", & tipo);
    
    if (tipo == 1){
         strcpy (ocupacao, "aluno");
    }
    
    else if (tipo == 2){
         strcpy (ocupacao, "professor");
    }
    
    if (tipo == 1 || tipo == 2){
        printf ("Acesso Liberado! Seja bem-vindo(a), %s.", ocupacao);
    }
    
    else {
        printf ("Acesso NEGADO!");
    }*/

    //Atividade 5.
    /*float venda;
    float presente;
    
    printf ("Qual valor você vendeu no total desse mês? ");
    scanf ("%f", & venda);
    
    if (venda < 0){
        printf ("É impossível vender um valor negativo");
    return 0;
    }
    
    if (venda >= 10000){
        presente = venda * 0.1;
        printf ("O vendedor ganhou 10%% do valor que vendeu, ou seja R$%.2f.", presente);
    }
    else {
        presente = venda * 0.05;
        printf ("O vendedor ganhou 5%% do valor que vendeu, ou seja R$%.2f.", presente);
    }*/

    //Atividade 6.
    /*int num;
    printf ("Digiteum número: ");
    scanf ("%d",& num);
    
    if (num % 2 == 0){
        printf ("O número %d é par! ", num);
    }
    else {
        printf ("O número %d é ímpar! ", num);
    }*/

    //Atividade 7.
    /*int A;
    int B;
    int C;

    int soma;

    printf ("Digite o valor de A: ");
    scanf ("%d", & A);
    printf ("Digite o valor de B: ");
    scanf ("%d", & B);
    printf ("Digite o valor de C: ");
    scanf ("%d", & C);

    soma = A + B;

    if (soma < C){
    printf ("%d (A+B) é menor que %d (C).", soma, C);
    }

    else if (soma = C){
    printf ("%d (A+B) é igual a %d (C).", soma, C);
    }

    else if (soma > C){
    printf ("%d (A+B) é maior que %d (C).", soma, C);
    }*/

    //Atividade 8.
    /*float num1;
    float num2;
    float num3;
    float MP;

    printf ("Peso do Primeiro Trimestre: 1\n");
    printf ("Peso do Segundo Trimestre: 2\n");
    printf ("Peso do Terceiro Trimestre: 3\n");

    printf ("Digite sua primeira nota: ");
    scanf ("%f", & num1);
    printf ("Digite sua segunda nota: ");
    scanf ("%f", & num2);
    printf ("Digite sua terceira nota: ");
    scanf ("%f", & num3);

    if (num1 < 0 || num1 > 100 || num2 < 0 || num2 > 100 || num3 < 0 || num3 > 100){
    printf ("Digite uma nota válida! ");
    return 0;
    }

    MP = ((num1 * 1) + (num2 * 2) + (num3 * 3)) / 6;

    if (MP >= 90){
    printf ("Sua média ponderada de %.2f é um A! Aprovado! ", MP);
    }
    else if (MP >= 75 && MP < 90){
    printf ("Sua média ponderada de %.2f é um B! Aprovado! ", MP);
    }
    else if (MP >= 60 && MP < 75){
    printf ("Sua média ponderada de %.2f é um C! Aprovado! ", MP);
    }
    else if (MP >= 40 && MP < 60){
    printf ("Sua média ponderada de %.2f é um D! Reprovado! ", MP);
    }
    else if (MP < 40){
    printf ("Sua média ponderada de %.2f é um E! Reprovado! ", MP);
    }*/
    
    return 0;
}
